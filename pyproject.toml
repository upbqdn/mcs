[tool.poetry]
name = "mcs"
version = "0.1.0"
description = ""
authors = ["Marek <mail@marek.onl>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31.0"
gpiod = "^2.2.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
